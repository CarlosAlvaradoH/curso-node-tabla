# Notas:

```
Opciones:
      --help      Muestra ayuda                                       [booleano]
      --version   Muestra número de versión                           [booleano]
  -b, --base      Es la base de la tabla de multiplicar               [número] [requerido]
  -m, --multiplo  Es el multiplo de la tabla de multiplicar           [número] [defecto: 10]
  -l, --listar    Muestra la tabla de multiplicar creada              [booleano] [defecto: false]
```
